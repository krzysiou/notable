import type { Config, TitleBarConfig } from './types';

const titleBarConfig: TitleBarConfig = {
  titleBarColor: '#f2f2f2',
  titleBarHeight: 30,
};

const defaultConfig: Config = {
  titleBarConfig,
};

export { defaultConfig };
